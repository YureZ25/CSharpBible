@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    // Это шаблон будет использован всеми представлениями по умолчанию, 
    // т.к. это файл _ViewStart.cshtml находится в корне папки Views

    // Назначение шаблона по убыванию приоритета:
    // В самом представлении
    // В файле _ViewStart в папке представления
    // В папке выше и т.д.
    // В папке Views
}
