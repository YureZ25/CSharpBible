@model string[]

@{
    ViewBag.Title = "Список языков";

    Layout = null; 
    // Внутри представления можно переназначить или обнулить шаблон
    // Здесь это имеет нивысший приоритет
}

<ul>
    @foreach (var lang in Model)
    {
        <li>@lang</li>
    }
</ul>
